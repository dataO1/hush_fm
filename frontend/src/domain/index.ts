/**
 * Domain Index
 * 
 * Centralized exports for all domain schemas.
 * Errors are defined within each specific domain schema for single source of truth.
 */

// Domain Schemas
export * from './schemas'