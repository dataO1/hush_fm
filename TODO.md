- [ ] fix WaveVisualizer component (currently commented out, since it breaks compilation)
- [ ] Separate listener and dj events/command in the backend
- [ ] Move handler functions to room logic and also create a lobby struct, which has the list of rooms etc and handles logic on the lobby instead of this directly in the websocket layer/handler

